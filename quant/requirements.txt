# 本工具核心功能 0 依赖，只用 Python 标准库，装好 Python 就能跑。
#
# 下面是【可选】的增强包，想要更强的数据源/画图能力时再装：
#   pip install -r requirements.txt
#
# yfinance      # 更稳定的美股/ETF/部分加密货币数据（备选数据源）
# pandas        # 表格数据处理，做更复杂的研究时好用
# matplotlib    # 本地静态画图（本工具默认用 HTML 图表，可不装）
# ccxt          # 统一对接上百家加密货币交易所的行情/下单接口（进阶实盘用）
