# Cloudflare Worker (static assets) for the Oddpeel B2C site.
# Set `name` to match your Oddpeel Worker/Pages project before deploying
# so the custom domain (e.g. www.oddpeel.com) stays attached.
name = "oddpeel-web"
compatibility_date = "2025-06-01"

[assets]
directory = "./"
