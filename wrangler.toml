# Cloudflare Pages — China West Journeys (static site, no build step)
# Connected to GitHub via the Cloudflare dashboard (Git integration).
name = "westours"
compatibility_date = "2025-06-01"
pages_build_output_dir = "."
